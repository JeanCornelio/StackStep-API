export enum UserFrom {
  GOOLE = 'google',
  GITHUB = 'github',
  DEFAULT = 'default',
}

export enum UserRoles {
  ADMIN = 'admin',
  USER = 'user',
}

export enum UserState {
  ACTIVE = 'active',
  DISABLED = 'disabled',
  PENDING_ACTIVATION = 'pending_activation',
}
