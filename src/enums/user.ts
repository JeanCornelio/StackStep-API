export enum UserFrom {
  GOOLE = 'google',
  GITHUB = 'github',
  DEFAULT = 'default',
}

export enum UserRoles {
  ADMIN = 'admin',
  USER = 'user',
}

//TODO: Create a user state
export enum UserState {
  ACTIVE = 'active',
  DISABLE = 'disabled',
  INACTIVE = 'inactive',
}
